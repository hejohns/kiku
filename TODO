1) make benchtests type requirement specific, not realized type specific
2) Containers need move behaviors (merge is ok, but what if we just want one node from list B appended
   to list A? Right now there is no way to do this without copying)
